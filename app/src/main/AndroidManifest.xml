<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.githubusers2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GitHubUserS2">

        <activity
            android:name=".ui.search.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.GitHubUserS2" />
        <activity
            android:name=".ui.screen.SplashScreen"
            android:exported="false"
            android:theme="@style/Theme.splashscreen"/>
        <activity
            android:name=".ui.home.HomeScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.detail.UserDetail"
            android:exported="false"
            android:theme="@style/Theme.GitHubUserS2" />
    </application>

</manifest>